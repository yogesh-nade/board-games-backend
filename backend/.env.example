# Environment Variables for Production
# Copy this file to .env and fill in the actual values

# Security
SECRET_KEY=your-super-secret-key-here-make-it-long-and-random
DEBUG=False

# Database
DATABASE_URL=your-render-postgresql-database-url

# Hosts - Using * for maximum flexibility (allows all hosts)
# In production, you can specify specific domains for better security
# ALLOWED_HOSTS=your-app-name.onrender.com,localhost,127.0.0.1

# CORS - Currently set to allow all origins in settings.py
# This allows access from localhost, deployed frontends, and any domain